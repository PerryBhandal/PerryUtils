set prefix C-a

unbind-key C-b
bind-key C-a send-prefix

set -s escape-time 0

bind n split-window -h # Split panes horizontal
bind s split-window -v # Split panes vertically

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
